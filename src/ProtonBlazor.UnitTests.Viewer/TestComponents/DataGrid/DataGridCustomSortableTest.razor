<ProDataGrid @ref="_dataGrid" Items="@_items" Sortable="true">
    <Columns>
        <PropertyColumn Property="x => x.Name" Comparer="@(_naturalSortingEnabled ? new ProtonBlazor.Utilities.NaturalComparer() : null)" />
        <PropertyColumn Property="x => x.Value" />
        <PropertyColumn Property="x => x.Misc" />
    </Columns>
</ProDataGrid>
<ProButton OnClick="CallSort" Variant="Variant.Outlined">Set Sort on Name Column</ProButton>
<ProSwitch @bind-Value="@_naturalSortingEnabled">Enable Natural Sorting</ProSwitch>

@code {
    public static string __description__ = @"Test in browser custom sorting of DataGrid. Note that the 'Name' column is being sorted using a natural sorting algorithm.";
    private ProDataGrid<Item> _dataGrid = null!;

    private bool _naturalSortingEnabled = true;

    private readonly IEnumerable<Item> _items = new List<Item>
    {
        new("1", 42, "555"), 
        new("10", 73, "7"), 
        new("2", 11, "4444"), 
        new("1_10", 33, "33333"),
        new("1_2", 99, "66"), 
        new("1_11", 44, "1111111"),
        new("0", 55, "222222")
    };

    private async Task CallSort()
    {
        await _dataGrid.SetSortAsync(nameof(Item.Name), SortDirection.Ascending, x => x.Name, new ProtonBlazor.Utilities.NaturalComparer());
    }

    public record Item(string Name, int Value, string Misc);
}
