@using ProtonBlazor.Utilities
<ProPopoverProvider />

<ProColorPicker ColorPickerMode="ColorPickerMode.HEX" ColorPickerView="@ColorPickerView" ShowToolbar="true" Label="Basic Color Picker" Placeholder="Select Color" @bind-Value="@_colorValue" />

@code {
	private ProColor? _colorValue;

	[Parameter]
	public ColorPickerView ColorPickerView { get; set; } = ColorPickerView.Grid;
}