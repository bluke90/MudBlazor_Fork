<ProSwipeArea OnSwipeEnd="@(e => _swipeDirection = e.SwipeDirection)" Style="width: 100%; height: 300px">
    <ProText Typo="@Typo.body1">@_swipeDirection</ProText>
</ProSwipeArea>

@code {
    private SwipeDirection _swipeDirection;
}