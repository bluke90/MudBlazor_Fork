@using ProtonBlazor.Interfaces 
@implements INavigationEventReceiver

<CascadingValue Value="this" IsFixed="true">
    <ProNavLink Disabled="@Disabled" />
</CascadingValue>

@code {
    [Parameter]
    public bool Disabled { get; set; }

    public bool IsNavigated { get; private set; }

    public Task OnNavigation()
    {
        IsNavigated = true;
        return Task.CompletedTask;
    }
}
