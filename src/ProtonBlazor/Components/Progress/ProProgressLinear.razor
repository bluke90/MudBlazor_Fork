@namespace ProtonBlazor
@using ProtonBlazor.Extensions
@inherits ProComponentBase

<div role="progressbar"
     aria-busy="@((Indeterminate || _valueState.Value < _maxState.Value).ToString().ToLowerInvariant())"
     aria-valuenow="@_valueState.Value.ToInvariantString()"
     aria-valuemin="@_minState.Value.ToInvariantString()"
     aria-valuemax="@_maxState.Value.ToInvariantString()"
     aria-live="@((Indeterminate ? null : "polite"))"
     class="@Classname" 
     style="@Style" 
     @attributes="UserAttributes">
    <div class="@BarsClassname">
        @if (Indeterminate)
        {
            <div class="pro-progress-linear-bar"></div>
            <div class="pro-progress-linear-bar"></div>
        }
        else if (Buffer)
        {
            <div class="pro-progress-linear-bar"></div>
            <div class="pro-progress-linear-bar" style="@GetStyledBar1Transform()"></div>
            <div class="pro-progress-linear-bar last" style="@GetStyledBar2Transform()"></div>
        }
        else
        {
            <div class="pro-progress-linear-bar" style="@GetStyledBar1Transform()"></div>
        }
    </div>

    @if (ChildContent is not null)
    {
        <div class="pro-progress-linear-content">
            @ChildContent
        </div>
    }
</div>
