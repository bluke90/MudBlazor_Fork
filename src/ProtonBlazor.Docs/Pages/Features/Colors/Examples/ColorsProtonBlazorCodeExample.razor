@namespace ProtonBlazor.Docs.Examples


<ProPaper Class="pa-4" Style="@($"color:{Theme.PaletteLight.Dark}; background:{Theme.PaletteLight.Warning};")">
    Warning with Dark text
</ProPaper>
<ProPaper Class="pa-4" Style="@($"color:{Theme.PaletteLight.SuccessLighten}; background:{Theme.PaletteLight.Dark};")">
    Dark with Success lighten
</ProPaper>

@code {
    private ProTheme Theme = new ProTheme();
}